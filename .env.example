# Server
NODE_ENV=development
PORT=3000

# Database (PostgreSQL) - when running app locally, use localhost and host port 5434
DB_HOST=localhost
DB_PORT=5434
DB_USERNAME=postgres
DB_PASSWORD=your_password
DB_DATABASE=task_management
